open the ece385_final qpf in quartus
select final_control as the top level entity if not set already
fully compile code
upload to fpga
open the nios II eclipse workspace
generate the given bsp
build project
run
attach vga

use the switches and buttons on the base fpga to manipulate the project
the left two switches select 1 of 4 sounds, the other 8 select the pattern for them
press the top button to save that pattern of that sound
press the bottom button to reset all